{
    "name": "adopt",
    "vendorUrl": "https://goadopt.io/",
    "prehideSelectors": ["#cookie-banner"],
    "detectCmp": [
        {
            "exists": "#adopt-controller-button"
        }
    ],
    "detectPopup": [
        {
            "visible": "#cookie-banner"
        }
    ],
    "optIn": [
        {
            "waitForThenClick": "#adopt-accept-all-button"
        }
    ],
    "optOut": [
        {
            "if": { "exists": "#adopt-reject-all-button" },
            "then": [{ "waitForThenClick": "#adopt-reject-all-button" }],
            "else": [{ "waitForThenClick": "#cookie-banner button:not([id])" }]
        }
    ],
    "test": [
        {
            "eval": "EVAL_ADOPT_TEST"
        }
    ]
}
