{
    "name": "pubtech",
    "prehideSelectors": ["#pubtech-cmp"],
    "detectCmp": [{ "exists": "#pubtech-cmp" }],
    "detectPopup": [{ "visible": "#pubtech-cmp" }],
    "optIn": [{ "click": "#pubtech-cmp #pt-accept-all" }],
    "optOut": [
        {"wait": 500},
        { "click": "#pubtech-cmp #pt-close:first-child" }
    ]
  }
  